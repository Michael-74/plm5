<form method="POST" action="/ol/sections/<%=id%>/add" id="sections__add_form">
    <div class="modal-header">
        <h3 class="modal-title text-left"><i class="fal fa-clipboard-list modal-icon"></i>&nbsp;&nbsp;Добавление раздела</h3>
    </div>
    <div class="modal-body">
        <div class="form-group">
            <label>Наименование раздела</label> <input name="name" type="text"  class="form-control">
            <span class="help-block">Например: <i>Требования к насосному оборудованию</i></span>
        </div>
    </div>
    <div class="modal-footer">
        <a href="#" class="btn btn-white" rel="modal:close">Закрыть</a>
        <button type="submit" class="btn btn-success">Добавить</button>
    </div>
</form>

<script>
$(function(){
    $('#sections__add_form').validate({
        submitHandler: function(form) {
            $(form).ajaxSubmit({
                success: function(data) {
                    $.modal.close();
                    if (data.ok === true) {
                        swal({
                            title: "Отлично!",
                            text: "Новый раздел добавлен!",
                            type: "success"
                        }, function () {
                            Tabs._updateCurrent();
                        });
                    } else {
                        swal("Опачки!", "Что-то пошло не так :)", "error");
                    }
                }
            });
        },
        rules: {
            name: {
                required: true,
            }
        }
    })
});
</script>